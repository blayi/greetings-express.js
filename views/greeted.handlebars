<h2>USERS GREETED</h2>
<div class="row">
      <div class="col-4">
        <a href="reset">
        <button class="button-primary" value="{{reset}}">Clear Name</button></a>
      </div>
       <div class="col-4">
           <form action="index" method="POST">
         <button class="button-primary">home</button></a>
    </form>

       </div>


    </div>
<table>
    <tr>
        <th>names</th>
        <th>counter</th>
    </tr>
    {{#each users}}
    <tr>
        <td><a href="/counter/{{name}}">{{name}}</a></td>
        <td>{{counter}}</td>
    </tr>
    {{/each}}
</table>

    
